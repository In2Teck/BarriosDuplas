<%= form_for(@additional_badge) do |f| %>
  <% if @additional_badge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@additional_badge.errors.count, "error") %> prohibited this additional_badge from being saved:</h2>

      <ul>
      <% @additional_badge.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
